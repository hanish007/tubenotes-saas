{
    "name": "tubenotes-root",
    "version": "1.0.0",
    "scripts": {
        "dev": "concurrently \"cd backend && python -m uvicorn main:app --reload --port 8000\" \"cd frontend && npm run dev\""
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}